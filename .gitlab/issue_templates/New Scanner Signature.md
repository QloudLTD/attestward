<!-- Add detection for a SAST/SCA/security scanner — a data-only contribution to
     mappings/scanner-signatures.yaml. Suggested label: enhancement -->

### Tool name and category

<!-- e.g. "Checkmarx One — SAST" or "OSV-Scanner — SCA" -->

### How it appears in workflows

<!-- The detectable fingerprint in CI workflows — action `uses:` slug(s), typical step
     names, CLI invocations in `run:` steps. Paste a real (sanitized) workflow snippet. -->

### Run-history mapping

<!-- How do its runs indicate a scan happened (job name, check run, artifact)? Anything
     needed to compute per-release cadence? -->

### Fixture

<!-- Can you provide a fixture workflow file for tests? -->
