# examples

| File | Purpose | Lands with |
|---|---|---|
| `attestor.yaml` | Fully-commented `attestor scan --config` example | issue #10 |
| demo-org evidence pack output | Sample `evidence.json`/`report.html` linked from the README quickstart | issue #15, #29 |

The demo-org pack isn't populated yet — that needs a real demo org (#15) and collectors
that actually run (#11+) to produce meaningful output.
