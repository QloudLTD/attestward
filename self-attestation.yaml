# Self-attestation answers for attestward (generated by `attestward attest init`).
#
# Fill in each `answer:` field below. Leaving one blank is fine — an
# unanswered question always reports not-checkable on the next scan, never
# assumed. See mappings/self-attestation-questions.yaml (version 1.0.0) for
# the full question set this file is a template of.

questions_version: "1.0.0"
answers:
  - id: SA.dev-security-training
    # Do developers with access to the software's source code or build pipeline receive periodic security training?
    # answer_type: yes_no_evidence -- answer must be "yes" or "no"
    # evidence_prompt: Link to the training policy/program, or a brief description of what it covers and how often it recurs
    answer: ""
    evidence_ref: ""
    attested_by: ""
    date: ""

  - id: SA.threat-modeling
    # Is the software's design reviewed — by a qualified person not involved in that design, and/or an automated process — to confirm it meets security requirements and addresses identified risk before implementation?
    # answer_type: yes_no_evidence -- answer must be "yes" or "no"
    # evidence_prompt: Link to the design/threat-model review process documentation, or a brief description of how it works
    answer: ""
    evidence_ref: ""
    attested_by: ""
    date: ""

  - id: SA.vuln-triage-sla
    # What is the producer's target time to triage (assess the risk of) a newly reported vulnerability?
    # answer_type: text
    # evidence_prompt: e.g. "5 business days" — cite the documented policy if one exists
    answer: ""
    evidence_ref: ""
    attested_by: ""
    date: ""

  - id: SA.vuln-remediation-sla
    # What is the producer's target time to remediate a confirmed vulnerability, by severity?
    # answer_type: text
    # evidence_prompt: e.g. "Critical: 7 days, High: 30 days" — cite the documented policy if one exists
    answer: ""
    evidence_ref: ""
    attested_by: ""
    date: ""

  - id: SA.agency-notification-process
    # Does the producer have a defined process to notify any agency this form was submitted to if it ceases consistent use of the attested practices? (Paraphrases the CISA form's own closing commitment; see cisa-ssda-form.yaml's notify_on_cessation_clause for the verbatim text this question is asking the producer to substantiate.)
    # answer_type: yes_no -- answer must be "yes" or "no"
    answer: ""
    evidence_ref: ""
    attested_by: ""
    date: ""

  - id: SA.audit-log-export-fallback
    # If GitHub's own audit-log API or streaming isn't available (e.g. a non-Enterprise plan, or streaming being an Enterprise-account-only feature this tool can't query — see C09.audit.log-streaming), does the producer export or retain audit/access logs through another mechanism, and for how long?
    # answer_type: yes_no_evidence -- answer must be "yes" or "no"
    # evidence_prompt: Describe the mechanism (e.g. SIEM export, third-party log retention) and its retention period
    answer: "no"
    evidence_ref: "DECISIONS.md D11 — solo-maintainer repo, no SIEM/webhook export exists; GitHub's own audit-log API is the only record and this is an accepted, deliberate gap, not an oversight"
    attested_by: "sioakim (repo owner)"
    date: "2026-07-19"

